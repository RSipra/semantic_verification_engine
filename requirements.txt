pandas==2.2.3
pydantic==2.12.0
pyfiglet==1.0.2
rich==14.0.0
numpy==2.0.2

sentence-transformers==5.6.0 
transformers==5.3.0 
rapidfuzz==3.14.3
regex==2026.5.9
word2number==1.1
dateparser==1.2.2
google-generativeai==0.8.5
pyarrow==24.0.0  # startup.py reads production parquet via pq.read_table() → df →
                 # pydantic hydration. Bumped from 19.0.0 for Arrow C++ IPC CVE
                 # (not exploitable — we use parquet reader, not the vulnerable IPC path).
pydantic-settings==2.12.0
python-dotenv==1.2.2
protobuf==5.29.6

# sentencepiece (older artifact)

# torch — NOT installed here. CPU-only build pinned in Dockerfile (torch==2.10.0,
# from download.pytorch.org/whl/cpu). Kept out of requirements.txt so sentence-transformers
# doesn't pull the default (CUDA) build over the CPU one - keep vm lean.
